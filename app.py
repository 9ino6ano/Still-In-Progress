import Program9
#This will import the file with the usefool tools file = program 9
#Python module index - (version) docx.
#Some modules are built in directly into python , some are under lib/
#installing using pip , which are  python-docx created by third parties resources
#This will be saved under site packages folder not lib/
print(Program9.roll_dice(10))

